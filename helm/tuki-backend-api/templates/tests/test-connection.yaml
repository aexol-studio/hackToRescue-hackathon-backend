apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "tuki-backend-api.fullname" . }}-test-connection"
  labels:
    {{- include "tuki-backend-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "tuki-backend-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
